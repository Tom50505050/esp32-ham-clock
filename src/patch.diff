diff --git a/main.cpp b/main.cpp
index 0000000..1111111 100644
--- a/main.cpp
+++ b/main.cpp
@@ void issTask(void *pvParameters) {
-      if (issDataMutex != NULL && xSemaphoreTake(issDataMutex, pdMS_TO_TICKS(100)) == pdTRUE) {
+      if (issDataMutex != NULL && xSemaphoreTake(issDataMutex, pdMS_TO_TICKS(50)) == pdTRUE) {
         fetchIssData();
         lastIssDataFetch = millis();
         xSemaphoreGive(issDataMutex);
       }
@@ void issTask(void *pvParameters) {
-      if (issDataMutex != NULL && xSemaphoreTake(issDataMutex, pdMS_TO_TICKS(100)) == pdTRUE) {
+      if (issDataMutex != NULL && xSemaphoreTake(issDataMutex, pdMS_TO_TICKS(50)) == pdTRUE) {
         getIssCountryFromCoords(issLat, issLng);
         lastIssCountryCheck = millis();
         xSemaphoreGive(issDataMutex);
       }
@@ void issTask(void *pvParameters) {
-    vTaskDelay(pdMS_TO_TICKS(100));
+    vTaskDelay(pdMS_TO_TICKS(50));
@@ void fetchIssData() {
-  http.setTimeout(2500);
+  http.setTimeout(1500);
@@ void fetchIssData() {
-    if (httpCode == 200) {
+    if (httpCode == HTTP_CODE_OK) {
@@ void fetchIssData() {
-    http.end();
+    http.end();
@@ void fetchIssData() {
+  http.end();
@@ void getIssCountryFromCoords(double lat, double lng) {
-  http.setTimeout(2500);
+  http.setTimeout(1500);
@@ void getIssCountryFromCoords(double lat, double lng) {
-  if (httpCode == 200) {
+  if (httpCode == HTTP_CODE_OK) {
@@ void getIssCountryFromCoords(double lat, double lng) {
+    http.end();
@@ void getIssCountryFromCoords(double lat, double lng) {
+    http.end();
@@ void updateIssDynamicDisplay() {
-  tft.setTextSize(2);
+  tft.setTextSize(1);
@@ void updateIssDynamicDisplay() {
+  String countryDisplay = localIssCountry;
+  if (countryDisplay.length() > 15) {
+    countryDisplay = countryDisplay.substring(0, 14) + "…";
+  }
@@ void setup() {
-    4096,
+    8192,
